cmake_minimum_required(VERSION 2.8)
project(histogram-calculation)
find_package(OpenCV REQUIRED)
add_executable(histogram-calculation histogram-calculation.cpp)
target_link_libraries(histogram-calculation ${OpenCV_LIBS})
